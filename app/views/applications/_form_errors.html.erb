<% if @application.errors.any? %>
  <div id="error_explanation">
    <h2>
      Please fix <%= pluralize(@application.errors.count, "error") %>:
    </h2>
    <ul>
      <% @application.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
